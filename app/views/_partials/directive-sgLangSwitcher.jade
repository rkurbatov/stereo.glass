div.lang-selector(style='z-index: 1000; position: fixed')
    ul
        li(ng-repeat='lang in INT.langs' ng-if='lang !== INT.currentLang')
            img(ng-src="{{ 'img/flags/' + lang + '.png' }}")
            span {{ lang }}