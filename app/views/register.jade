extends layout
block content
    .container: .row: .col-sm-6.col-md-4.col-md-offset-4
        h2.text-center Регистрация
        form(role='form', action="/auth/register", method="post")
            .form-group
                input.form-control(name="username", type='email', required, autofocus, placeholder='адрес Email')
            .form-group
                input.form-control(type='password', name="password", required, placeholder='Пароль')
            .form-actions.text-center
                br
                a(href="/auth") У меня уже есть учетная запись
                br
                br
                button.btn.btn-default.text-center(type='submit') Регистрация